// ★ 流水线引擎(M3 阶段)——核心差异化能力。
//
// 子模块:
//   model.rs  —— 数据类型(与 D 的前端编辑器共享 schema)
//   executor.rs  —— 执行引擎(A-6,待实现)
//   registry.rs —— 节点类型注册表(A-7,待实现)
//   preview.rs  —— Dry-run 预览(A-8,待实现)

pub mod model;
